abstract final class AppConstants {
  static const appName = 'Mufeed';
  static const orgName = 'com.mufeedapp';

  // Min supported platforms
  static const minIosVersion = 15;
  static const minAndroidApi = 24;

  // Max app size
  static const maxAppSizeMb = 50;

  // Performance targets
  static const maxSessionLoadMs = 2000;
  static const maxTransitionMs = 100;
  static const maxTtsStartMs = 500;
  static const maxStartupMs = 3000;
}
